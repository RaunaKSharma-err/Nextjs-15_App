import React from "react";

const IdLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <>
      <div>{children}</div> featured product
    </>
  );
};

export default IdLayout;
